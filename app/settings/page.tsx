import AdminSectionPage from "@/components/layout/AdminSectionPage"

export default function SettingsPage() {
  return (
    <AdminSectionPage
      eyebrow="Settings"
      title="Overview"
      description="Manage platform configuration, admin permissions, integrations, and workspace defaults."
    />
  )
}
